alertmanager:
  enabled: true
kubeStateMetrics:
  enabled: false
prometheus-node-exporter:
  enabled: false
prometheus-pushgateway:
  enabled: false

server:
  ingress:
    enabled: true
    className: null
    hosts:
    - prometheus.127.0.0.1
    paths: /
  persistentVolume:
    enabled: false
